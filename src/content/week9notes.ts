// "9주차 적용 포인트" 팝업 본문. Streamlit(그룹웨어영업-report)과 이 Vercel 앱의 관계, Day3~5에서
// 실제로 적용된 지점을 설명한다. 내용을 바꿀 때는 문장만 고치면 된다 — 로직과 무관한 정적 텍스트.
export const WEEK9_NOTES_HTML = `
<h3>이 앱은 무엇인가</h3>
<p>원래 대시보드(세그먼트·채널·라인업·리텐션 4페이지 + 제안서)는 Streamlit(Python)으로 만들었다.
이 앱은 그중 <strong>제안서 생성 기능만</strong> 정적 웹앱으로 옮긴 것이다 — 대시보드 자체는 여전히 Streamlit에 있다.</p>

<table>
  <thead><tr><th></th><th>Streamlit (원본 대시보드)</th><th>이 앱 (Vercel)</th></tr></thead>
  <tbody>
    <tr><td>계산이 일어나는 곳</td><td>매 상호작용마다 서버가 pandas로 재계산</td><td>빌드 전에 이미 끝남 — Python이 한 번 계산해 JSON으로 내보낸 것을 읽기만 함</td></tr>
    <tr><td>서버</td><td>Python 프로세스가 항상 떠 있어야 함</td><td>없음 — 정적 파일만 CDN에 있음</td></tr>
    <tr><td>데이터 갱신</td><td>원본 데이터만 바꾸면 즉시 반영</td><td>Python에서 다시 내보내고 재배포해야 반영</td></tr>
  </tbody>
</table>

<h3>Day3 — 제안서 만들기</h3>
<ul>
  <li>현황→원인→규모→제안→위험·철회 기준→요청, 6절 구조를 그대로 옮김</li>
  <li>"채울 함수를 못 대면 절을 안 만든다" — 근거가 없는 절은 화면에도 나타나지 않음</li>
  <li>"기각된 후보도 지우지 않는다" — 드롭다운에 15개 주제 전부 있고, 채널 축 후보들은 카이제곱 검정 p-value 사유와 함께 "(차이 없음)"으로 남아 있음</li>
  <li>"그림 하나에 문장 하나, 색은 3개까지" — 차트 3종(현황·원인·규모)에만, 강조·위험·회색 3색 제한 유지</li>
</ul>

<h3>Day4 — 이 제안서로 결정이 나오는가</h3>
<ul>
  <li>자가 검사(빈칸·절 개수·요청 절 결정 동사)를 실시간으로 재실행 — 위험/요청 칸에 입력할 때마다 배지가 바로 바뀜</li>
  <li>실제 페르소나 판정(프로덕트/그로스 팀장, 판정: 채택)에서 고친 내용이 카드 데이터에 그대로 들어 있음 — 확신도 표기, 비용을 확정치 대신 "확인 계획" 형식으로, 매출환산에 가정 사슬을 전부 명시</li>
  <li>지적을 A(고친다)/B(위치만 옮긴다)/C(안 고친다+이유)로 나눈 결과도 이 카드 문장에 반영됨</li>
</ul>

<h3>Day5 — 9주차 마무리</h3>
<ul>
  <li>"앱은 데이터가 바뀌면 고쳐야 하고, 판단 기준은 안 바뀐다" — 이번엔 계산(Python)과 화면(TS) 두 곳으로 나뉘어서, 데이터가 바뀌면 두 곳을 순서대로 갱신해야 함(README에 동기화 표로 명시)</li>
  <li>이 앱은 AI가 채점하는 벤치마크 앱이 아니라, 이미 검증한 계산을 문서로 조립해줄 뿐이다 — <strong>제안이 실제로 옳은지는 여전히 A/B 테스트가 답할 몫</strong>이라는 원칙은 그대로 유지된다</li>
</ul>
`;
